#include <stdio.h>
/**
 * main - main entry point
 * Description: prints
 * Return: returns 0 (always success)
 */

int main(void)
{
	int x;

	for (x = 'a'; x <= 'z'; x++)
	{
	putchar(x);
	}
	putchar('\n');
return (0);
}
