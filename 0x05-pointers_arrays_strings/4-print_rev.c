#include "main.h"
#include <stdio.h>
/**
 * print_rev - a function that prints a string in reverse followed by a new line * @s: input parameter
 * return: returns string in reverse
 */
void print_rev(char *s)
{
	int i;

	for (i = '\0'; s[i] = 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
