#include "main.h"
#include <stdio.h>
/**
 * print_array - prints n elements of an array of integers
 * @a: input integer
 * @n: input integer
 * return: returns prints of element of an array
 */
void print_array(int *a, int n)
{
	int a[];
	int i = 0;

	for (i = 0; i < n; i++)

	printf("%d, ", a[i]);
	printf("\n");
}

