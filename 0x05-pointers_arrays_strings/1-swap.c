#include "main.h"
#include <stdio.h>
/**
 * swap_int - swaps the value of two integers
 * Description: the integer parameters are as seen below
 * @a: first integer
 * @b: second integer
 * Return: returns void
 */
void swap_int(int *a, int *b)
{

	int i;

	i = *a;
	*a = *b;
	*b = i

}
