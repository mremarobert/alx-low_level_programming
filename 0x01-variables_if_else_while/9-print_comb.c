#include <stdio.h>
/**
 * main - entry point
 * Description: prints alphabets
 * Return: returns success
 */
int main(void)
{
	int a = 0;

	for (a = 48; a <= 57; a++)
	{
	putchar(a);
	{
	putchar(44);
	putchar(32);
	}
	}
	putchar('\n');
return (0);
}
