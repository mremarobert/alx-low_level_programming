#include <stdio.h>
/**
 * main - entry point
 * Description: prints alphabets
 * Return: returns success
 */
int main(void)
{
	int a = 0;

	for (a = 0; a <= 9; a++)
	{
	putchar(a);
	}
	putchar('\n');
return (0);
}
