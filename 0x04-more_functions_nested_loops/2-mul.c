#include "main.h"
/**
 * mul - multiplies to integers
 * @a: input parameter to the function
 * @b: input parameter to the function
 * Return: returns multiples of two integers
 */
int mul(int a, int b)
{
	return (a * b);

}
